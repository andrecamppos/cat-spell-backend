---
phase: 7
plan: 01
title: "Schema & Model Foundation"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V13__make_swipe_cat_id_nullable.sql
  - src/main/kotlin/com/catspell/api/discovery/model/Swipe.kt
  - src/main/kotlin/com/catspell/api/discovery/model/FeedProjection.kt
  - src/main/kotlin/com/catspell/api/discovery/model/DiscoveryDtos.kt
  - src/main/kotlin/com/catspell/api/common/exception/Exceptions.kt
  - src/main/kotlin/com/catspell/api/common/exception/GlobalExceptionHandler.kt
autonomous: true
requirements:
  - DISC-08
  - DISC-09
  - DISC-10
  - DISC-11
---

<objective>
Create the structural foundation for mixed discovery feed: database migration to make cat_id nullable with partial unique indexes, Swipe entity update, polymorphic FeedProjection, updated DTOs with type discriminator and nullable cat fields, and updated SwipeRequest with optional catId/targetUserId. Generalize exception messages for cat-agnostic swipe context.
</objective>

<must_haves>
- Flyway V13 migration runs cleanly on existing data (all existing swipes have cat_id set)
- Swipe entity catProfile becomes nullable without breaking existing swipe saves
- FeedProjection includes type discriminator and nullable cat fields for UNION query compatibility
- FeedItemResponse includes type field (CAT/HUMAN) and nullable cat-specific fields
- FeedResponse field renamed from `cats` to `cards`
- SwipeRequest accepts optional catId or targetUserId (not both)
- Exception messages are cat-agnostic

truths:
  - D-05: Single list with type discriminator — FeedResponse.cards: List<FeedItem> with type field (CAT or HUMAN)
  - D-06: Field name `cards` (was `cats`) in FeedResponse — breaking change accepted
  - D-09: SwipeRequest has two optional fields catId and targetUserId with exactly-one validation
  - D-10: cat_id becomes nullable in swipes table, two partial unique indexes replace single unique index
</must_haves>

<tasks>

<task id="07-01-01">
<title>Create Flyway V13 migration for nullable cat_id</title>
<type>execute</type>
<files>src/main/resources/db/migration/V13__make_swipe_cat_id_nullable.sql</files>

<read_first>
- src/main/resources/db/migration/V8__create_swipes_table.sql
</read_first>

<action>
Create new migration file V13__make_swipe_cat_id_nullable.sql with:
1. ALTER TABLE swipes ALTER COLUMN cat_id DROP NOT NULL
2. DROP INDEX idx_swipes_unique (the current swiper_id, cat_id unique index)
3. CREATE UNIQUE INDEX idx_swipes_unique_cat ON swipes(swiper_id, cat_id) WHERE cat_id IS NOT NULL
4. CREATE UNIQUE INDEX idx_swipes_unique_human ON swipes(swiper_id, target_user_id) WHERE cat_id IS NULL
</action>

<acceptance_criteria>
- File exists at src/main/resources/db/migration/V13__make_swipe_cat_id_nullable.sql
- Contains ALTER TABLE swipes ALTER COLUMN cat_id DROP NOT NULL
- Contains DROP INDEX idx_swipes_unique
- Contains CREATE UNIQUE INDEX idx_swipes_unique_cat with WHERE cat_id IS NOT NULL
- Contains CREATE UNIQUE INDEX idx_swipes_unique_human with WHERE cat_id IS NULL
- Migration runs without error on existing data (all existing rows have cat_id set)
</acceptance_criteria>

<verify>
<automated>./gradlew test --tests "com.catspell.api.discovery.*" 2>&1 | tail -5</automated>
</verify>
</task>

<task id="07-01-02">
<title>Update Swipe entity for nullable catProfile</title>
<type>execute</type>
<files>src/main/kotlin/com/catspell/api/discovery/model/Swipe.kt</files>

<read_first>
- src/main/kotlin/com/catspell/api/discovery/model/Swipe.kt
</read_first>

<action>
Change catProfile field from CatProfile to CatProfile? (nullable):
- @JoinColumn(name = "cat_id", nullable = true) — was nullable = false
- var catProfile: CatProfile? — was CatProfile (non-null)
Keep all other fields unchanged. Constructor parameter type changes accordingly.
</action>

<acceptance_criteria>
- Swipe.kt contains `var catProfile: CatProfile?` (nullable type)
- @JoinColumn annotation has nullable = true
- Existing Swipe creation code (with catProfile set) still compiles
- No other fields in Swipe entity are modified
</acceptance_criteria>

<verify>
<automated>./gradlew compileKotlin 2>&1 | tail -5</automated>
</verify>
</task>

<task id="07-01-03">
<title>Update FeedProjection for polymorphic feed</title>
<type>execute</type>
<files>src/main/kotlin/com/catspell/api/discovery/model/FeedProjection.kt</files>

<read_first>
- src/main/kotlin/com/catspell/api/discovery/model/FeedProjection.kt
</read_first>

<action>
Replace FeedProjection interface with polymorphic version supporting both CAT and HUMAN cards:
- Add fun getType(): String — returns "CAT" or "HUMAN"
- Rename getCatId() to nullable: fun getCatId(): UUID?
- Rename getName() to fun getCatName(): String? (nullable for HUMAN cards)
- Rename getAge() to fun getCatAge(): Int? (nullable, use Integer boxed return for Spring Data null support)
- Rename getAgeUnit() to fun getCatAgeUnit(): String?
- Rename getBio() to fun getCatBio(): String?
- Rename getBreed() to fun getCatBreed(): String? (already nullable, keep)
- Rename getCatPhotoThumbnail() stays as fun getCatPhotoThumbnail(): String?
- Rename getOwnerId() to fun getUserId(): UUID — non-null, represents owner for CAT, user for HUMAN
- Rename getOwnerDisplayName() to fun getDisplayName(): String — non-null for both types
- Rename getOwnerPhotoThumbnail() to fun getUserPhotoThumbnail(): String?
- Keep fun getDistanceKm(): Int — non-null for both types
</action>

<acceptance_criteria>
- FeedProjection.kt contains fun getType(): String
- FeedProjection.kt contains fun getCatId(): UUID? (nullable return)
- FeedProjection.kt contains fun getCatName(): String?
- FeedProjection.kt contains fun getUserId(): UUID
- FeedProjection.kt contains fun getDisplayName(): String
- FeedProjection.kt contains fun getUserPhotoThumbnail(): String?
- FeedProjection.kt contains fun getDistanceKm(): Int
- All cat-specific getters return nullable types
</acceptance_criteria>

<verify>
<automated>./gradlew compileKotlin 2>&1 | tail -5</automated>
</verify>
</task>

<task id="07-01-04">
<title>Update discovery DTOs for mixed feed</title>
<type>execute</type>
<files>src/main/kotlin/com/catspell/api/discovery/model/DiscoveryDtos.kt</files>

<read_first>
- src/main/kotlin/com/catspell/api/discovery/model/DiscoveryDtos.kt
</read_first>

<action>
Update three data classes in DiscoveryDtos.kt:

1. SwipeRequest — replace single catId with two optional fields:
   - val catId: UUID? = null (was @NotNull val catId: UUID)
   - val targetUserId: UUID? = null (new field)
   - val action: String — keep @NotNull @Pattern(LIKE|PASS) unchanged
   - Remove @NotNull from catId

2. FeedItemResponse — add type discriminator and make cat fields nullable:
   - Add val type: String (first field — "CAT" or "HUMAN")
   - Rename catId to nullable: val catId: UUID? = null
   - Rename name to catName: val catName: String? = null
   - Rename age to catAge: val catAge: Int? = null
   - Rename ageUnit to catAgeUnit: val catAgeUnit: String? = null
   - Keep breed as val breed: String? = null (already nullable)
   - Rename bio to catBio: val catBio: String? = null
   - Rename ownerId to userId: val userId: UUID
   - Rename ownerDisplayName to displayName: val displayName: String
   - Rename catPhotoThumbnail to val catPhotoThumbnail: String? = null
   - Rename ownerPhotoThumbnail to userPhotoThumbnail: val userPhotoThumbnail: String? = null
   - Keep distanceKm: val distanceKm: Int

3. FeedResponse — rename field:
   - val cards: List<FeedItemResponse> (was val cats)
   - val cursor: CursorResponse? — unchanged

Keep CursorResponse, FeedRequest, OwnerProfileResponse, OwnerPhotoResponse, OwnerCatSummary, SwipeResponse unchanged.
</action>

<acceptance_criteria>
- SwipeRequest has val catId: UUID? = null (nullable, no @NotNull)
- SwipeRequest has val targetUserId: UUID? = null
- SwipeRequest still has @NotNull @Pattern on action field
- FeedItemResponse has val type: String as first field
- FeedItemResponse has val catId: UUID? = null (nullable)
- FeedItemResponse has val catName: String? = null
- FeedItemResponse has val userId: UUID (renamed from ownerId)
- FeedItemResponse has val displayName: String (renamed from ownerDisplayName)
- FeedItemResponse has val userPhotoThumbnail: String? = null
- FeedResponse has val cards: List<FeedItemResponse> (was cats)
- All unchanged DTOs (CursorResponse, SwipeResponse, OwnerProfileResponse, etc.) remain identical
</acceptance_criteria>

<verify>
<automated>./gradlew compileKotlin 2>&1 | tail -5</automated>
</verify>
</task>

<task id="07-01-05">
<title>Generalize exception messages for mixed swipe context</title>
<type>execute</type>
<files>
- src/main/kotlin/com/catspell/api/common/exception/Exceptions.kt
- src/main/kotlin/com/catspell/api/common/exception/GlobalExceptionHandler.kt
</files>

<read_first>
- src/main/kotlin/com/catspell/api/common/exception/Exceptions.kt
- src/main/kotlin/com/catspell/api/common/exception/GlobalExceptionHandler.kt
</read_first>

<action>
In Exceptions.kt:
- DuplicateSwipeException default message: "Already swiped on this profile" (was "Already swiped on this cat")
- SelfSwipeException default message: "Cannot swipe on yourself" (was "Cannot swipe on your own cat")

In GlobalExceptionHandler.kt:
- handleDuplicateSwipe fallback message: "Already swiped on this profile" (was "Already swiped on this cat")
- handleSelfSwipe fallback message: "Cannot swipe on yourself" (was "Cannot swipe on your own cat")
</action>

<acceptance_criteria>
- Exceptions.kt DuplicateSwipeException default message is "Already swiped on this profile"
- Exceptions.kt SelfSwipeException default message is "Cannot swipe on yourself"
- GlobalExceptionHandler.kt handleDuplicateSwipe fallback is "Already swiped on this profile"
- GlobalExceptionHandler.kt handleSelfSwipe fallback is "Cannot swipe on yourself"
</acceptance_criteria>

<verify>
<automated>grep -n "swiped on this\|swipe on your" src/main/kotlin/com/catspell/api/common/exception/Exceptions.kt src/main/kotlin/com/catspell/api/common/exception/GlobalExceptionHandler.kt</automated>
</verify>
</task>

</tasks>

<verification>
After all tasks complete:
1. `./gradlew compileKotlin` exits 0 — all structural changes compile
2. Flyway migration V13 is valid SQL
3. FeedProjection getters match expected UNION query column aliases
4. DTO field renames are consistent (no orphaned references)
</verification>

<success_criteria>
- [ ] V13 migration makes cat_id nullable with partial unique indexes
- [ ] Swipe entity accepts null catProfile
- [ ] FeedProjection supports both CAT and HUMAN card types
- [ ] DTOs have type discriminator, nullable cat fields, renamed fields
- [ ] Exception messages are cat-agnostic
- [ ] Project compiles cleanly
</success_criteria>

## Artifacts this phase produces

### New files
- `src/main/resources/db/migration/V13__make_swipe_cat_id_nullable.sql` — Flyway migration

### Modified symbols
- `Swipe.catProfile` — type changes from `CatProfile` to `CatProfile?`
- `FeedProjection` — interface rewritten with `getType()`, nullable cat getters, renamed user getters
- `SwipeRequest.catId` — type changes from `UUID` to `UUID?`, `@NotNull` removed
- `SwipeRequest.targetUserId` — new field `UUID?`
- `FeedItemResponse.type` — new field `String`
- `FeedItemResponse.catName` — renamed from `name`, now `String?`
- `FeedItemResponse.catAge` — renamed from `age`, now `Int?`
- `FeedItemResponse.catAgeUnit` — renamed from `ageUnit`, now `String?`
- `FeedItemResponse.catBio` — renamed from `bio`, now `String?`
- `FeedItemResponse.userId` — renamed from `ownerId`
- `FeedItemResponse.displayName` — renamed from `ownerDisplayName`
- `FeedItemResponse.userPhotoThumbnail` — renamed from `ownerPhotoThumbnail`
- `FeedResponse.cards` — renamed from `cats`
- `DuplicateSwipeException` — default message changed
- `SelfSwipeException` — default message changed
