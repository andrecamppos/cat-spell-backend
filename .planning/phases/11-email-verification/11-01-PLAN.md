---
phase: 11-email-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt
  - src/main/kotlin/com/catspell/api/auth/model/EmailVerificationTokenRepository.kt
  - src/main/kotlin/com/catspell/api/auth/model/User.kt
  - src/main/resources/db/migration/V16__create_email_verification_tokens_table.sql
  - src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql
autonomous: true
requirements: [VERIFY-02, VERIFY-05]
must_haves:
  truths:
    - "An email_verification_tokens table exists (via Flyway V16) with a UUID PK, a user_id FK to users(id) ON DELETE CASCADE, a unique token_hash column, an expires_at TIMESTAMPTZ, a nullable used_at TIMESTAMPTZ, and a created_at TIMESTAMPTZ — mirroring password_reset_tokens / V15 (D-07)."
    - "The EmailVerificationToken JPA entity @Column names/types exactly match V16 so Hibernate ddl-auto validate agrees on the schema (no drift, app boots)."
    - "EmailVerificationTokenRepository exposes findByTokenHash(tokenHash), findAllByUserAndUsedAtIsNull(user), and an atomic @Modifying markUsed(id, now) conditional UPDATE (usedAt IS NULL guard) that closes the read-check-write race (VERIFY-02 single-use)."
    - "Flyway V17 adds a nullable email_verified_at TIMESTAMPTZ column to users (NULL = unverified, a value = verified-at instant) per D-06, and User.emailVerifiedAt maps it as a nullable Instant."
    - "V17 grandfathers every pre-existing users row by setting email_verified_at where it IS NULL, so no current account is locked out on rollout (VERIFY-05, D-09); backfill value is created_at (Claude's discretion per D-09)."
    - "Flyway migrations apply automatically on app boot (bootRun) and on Testcontainers startup — there is no separate schema-push step (Flyway stack)."
    # Edge (VERIFY-05 / empty) — covered
    - "Running V17 against an empty users table is a no-op UPDATE that still completes successfully (0 rows affected, migration succeeds)."
    # Edge (VERIFY-05 / adjacency) — covered
    - "V17 only touches rows whose email_verified_at IS NULL (all pre-existing rows at migration time); the WHERE IS NULL guard makes the backfill idempotent and prevents overwriting any already-set value."
    # Edge (VERIFY-05 / ordering) — covered
    - "The grandfather backfill is a single set-based UPDATE, so its outcome is deterministic and independent of row order or row count."
    # Edge (VERIFY-02 / precision) — covered
    - "token_hash stores a deterministic fixed-width SHA-256 hex digest, so an identical raw token always maps to the same stored value and is found by one unique-index lookup (no collision/precision ambiguity)."
  prohibitions:
    - "Never store the raw verification token in the database — only the SHA-256 hash lives in token_hash."
    - "Never make token_hash nullable or non-unique — single-use redemption depends on a unique indexed lookup."
    - "Never write a grandfather backfill that leaves any pre-existing user with a NULL email_verified_at (that would lock them out — VERIFY-05)."
    - "Never make email_verified_at NOT NULL — new users must register as unverified (NULL)."
  artifacts:
    - path: "src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt"
      provides: "JPA entity for the hashed single-use verification token"
      contains: "email_verification_tokens"
    - path: "src/main/kotlin/com/catspell/api/auth/model/EmailVerificationTokenRepository.kt"
      provides: "Spring Data repository with findByTokenHash + atomic markUsed"
      contains: "findByTokenHash"
    - path: "src/main/kotlin/com/catspell/api/auth/model/User.kt"
      provides: "email_verified_at entity field (nullable Instant)"
      contains: "emailVerifiedAt"
    - path: "src/main/resources/db/migration/V16__create_email_verification_tokens_table.sql"
      provides: "Flyway migration creating email_verification_tokens + indexes"
      contains: "CREATE TABLE email_verification_tokens"
    - path: "src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql"
      provides: "Flyway migration adding email_verified_at + grandfather backfill"
      contains: "email_verified_at"
  key_links:
    - from: "src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt"
      to: "src/main/resources/db/migration/V16__create_email_verification_tokens_table.sql"
      via: "entity @Column names/types must match migration DDL (schema parity)"
      pattern: "token_hash"
    - from: "src/main/kotlin/com/catspell/api/auth/model/User.kt"
      to: "src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql"
      via: "emailVerifiedAt @Column must match the V17 email_verified_at column (ddl-auto validate)"
      pattern: "email_verified_at"
    - from: "src/main/kotlin/com/catspell/api/auth/model/EmailVerificationTokenRepository.kt"
      to: "src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt"
      via: "JpaRepository<EmailVerificationToken, UUID>"
      pattern: "EmailVerificationToken"
---

<objective>
Create the durable, hashed, single-use verification-token store plus the `email_verified_at`
verified-state column and grandfather migration. This is a near-exact mirror of Phase 10's
`PasswordResetToken` + `V15` layer: `EmailVerificationToken` entity + repository (VERIFY-02
single-use), Flyway `V16` (token table) and `V17` (add `users.email_verified_at` + backfill so
no existing user is locked out — VERIFY-05, D-06/D-09).

Purpose: Give Plan 03's service a place to persist an issued token's hash and to claim it atomically,
and give the login gate a column to read.
Output: `auth/model/EmailVerificationToken.kt`, `auth/model/EmailVerificationTokenRepository.kt`,
the `email_verified_at` field on `User.kt`, `V16__create_email_verification_tokens_table.sql`,
`V17__add_email_verified_at_to_users.sql`.
</objective>

<execution_context>
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/workflows/execute-plan.md
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-email-verification/11-CONTEXT.md
@.planning/phases/11-email-verification/11-PATTERNS.md
</context>

<artifacts_this_phase>
Full symbol inventory for Phase 11 (this plan produces the DB-layer subset, marked ★):
- ★ `EmailVerificationToken` entity (auth/model) + `email_verification_tokens` table
- ★ `EmailVerificationTokenRepository` (findByTokenHash, findAllByUserAndUsedAtIsNull, markUsed)
- ★ `email_verified_at` TIMESTAMPTZ column on `users` + `User.emailVerifiedAt` field
- ★ `V16__create_email_verification_tokens_table.sql`
- ★ `V17__add_email_verified_at_to_users.sql` (add column + grandfather backfill)
- `EmailVerificationEmailRenderer` (email/service) + `app.verify-email-url` config key (Plan 02)
- `EmailVerificationService` (issue/resend) + config keys `app.verify-token.ttl-hours`, `app.resend-verification.per-email-capacity`, `app.resend-verification.per-email-refill-hours` (Plan 03)
- `AuthService.verifyEmail(rawToken)`; modified `AuthService.register` (no-token 201) + `AuthService.login` (verified gate) (Plan 03)
- `EmailNotVerifiedException` + `GlobalExceptionHandler.handleEmailNotVerified` (403, code EMAIL_NOT_VERIFIED) (Plan 03)
- `VerifyEmailRequest`, `ResendVerificationRequest` DTOs (Plan 04)
- `POST /api/auth/verify-email`, `POST /api/auth/resend-verification`; modified `POST /api/auth/register` (201 GenericMessageResponse) (Plan 04)
- Whitelist additions in SecurityConfig, JwtAuthenticationFilter, RateLimitFilter.AUTH_PATHS (Plan 03)
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Create EmailVerificationToken entity + repository (mirror PasswordResetToken)</name>
  <files>src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt, src/main/kotlin/com/catspell/api/auth/model/EmailVerificationTokenRepository.kt</files>
  <read_first>
    - src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt (exact template: UUID PK, LAZY @ManyToOne user, token_hash unique, expires_at, nullable used_at, created_at, id-based equals/hashCode)
    - src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt (exact template: findByTokenHash, findAllByUserAndUsedAtIsNull, @Modifying @Query markUsed)
    - src/main/kotlin/com/catspell/api/auth/model/User.kt (the FK target)
    - .planning/phases/11-email-verification/11-PATTERNS.md (EmailVerificationToken.kt / repository copy notes, lines 169-221)
  </read_first>
  <action>
    Create `@Entity @Table(name = "email_verification_tokens") class EmailVerificationToken` in package
    `com.catspell.api.auth.model`, copying `PasswordResetToken.kt` structure verbatim and renaming the class:
    `@Id @GeneratedValue(strategy = GenerationType.UUID) var id: UUID? = null`; `@ManyToOne(fetch =
    FetchType.LAZY) @JoinColumn(name = "user_id", nullable = false) var user: User`; `@Column(name =
    "token_hash", nullable = false, unique = true) var tokenHash: String`; `@Column(name = "expires_at",
    nullable = false) var expiresAt: Instant`; `@Column(name = "used_at") var usedAt: Instant? = null`;
    `@Column(name = "created_at", nullable = false, updatable = false) var createdAt: Instant = Instant.now()`.
    Keep the id-based `equals`/`hashCode` override verbatim. Store ONLY the hash — there is no raw-token column.
    Create `interface EmailVerificationTokenRepository : JpaRepository<EmailVerificationToken, UUID>` with
    `findByTokenHash(tokenHash: String): EmailVerificationToken?`, `findAllByUserAndUsedAtIsNull(user: User):
    List<EmailVerificationToken>`, and the atomic single-use guard `@Modifying @Query("UPDATE
    EmailVerificationToken t SET t.usedAt = :now WHERE t.id = :id AND t.usedAt IS NULL") fun markUsed(@Param("id")
    id: UUID, @Param("now") now: Instant): Int`. The `usedAt IS NULL` guard is essential — it closes the
    read-check-write race so concurrent verify calls claim the token exactly once (VERIFY-02 single-use, D-07).
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: entity is annotated `@Table(name = "email_verification_tokens")` with columns token_hash (unique, non-null), expires_at (non-null), used_at (nullable), user_id FK, created_at.
    - Source assertion: no raw-token column exists (only tokenHash).
    - Source assertion: repository exposes findByTokenHash (nullable return), findAllByUserAndUsedAtIsNull, and a @Modifying markUsed returning Int with a `usedAt IS NULL` WHERE guard.
    - `./gradlew compileKotlin` succeeds.
  </acceptance_criteria>
  <done>The hashed single-use verification-token entity + repository compile and expose an indexed findByTokenHash and an atomic markUsed (VERIFY-02).</done>
</task>

<task type="auto">
  <name>Task 2: Create V16 + V17 Flyway migrations and add User.emailVerifiedAt</name>
  <files>src/main/resources/db/migration/V16__create_email_verification_tokens_table.sql, src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql, src/main/kotlin/com/catspell/api/auth/model/User.kt</files>
  <read_first>
    - src/main/resources/db/migration/V15__create_password_reset_tokens_table.sql (exact DDL template — highest existing migration; next are V16, V17)
    - src/main/kotlin/com/catspell/api/auth/model/User.kt (timestamp column style: created_at / updated_at nullable-Instant mappings)
    - src/main/kotlin/com/catspell/api/auth/model/EmailVerificationToken.kt (Task 1 — columns MUST match V16)
    - .planning/phases/11-email-verification/11-PATTERNS.md (Migrations section lines 504-543; Flyway version status lines 15-24)
  </read_first>
  <action>
    Create `V16__create_email_verification_tokens_table.sql` (next after existing V15) mirroring V15:
    `CREATE TABLE email_verification_tokens (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), user_id UUID NOT NULL
    REFERENCES users(id) ON DELETE CASCADE, token_hash VARCHAR(255) NOT NULL UNIQUE, expires_at TIMESTAMP WITH
    TIME ZONE NOT NULL, used_at TIMESTAMP WITH TIME ZONE, created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT
    NOW());` plus `CREATE INDEX idx_email_verification_tokens_user_id ON email_verification_tokens(user_id);` and
    `CREATE UNIQUE INDEX idx_email_verification_tokens_token_hash ON email_verification_tokens(token_hash);`.
    Column names/types MUST exactly match the Task 1 entity @Column mappings so Hibernate ddl-auto validate
    passes on boot.
    Create `V17__add_email_verified_at_to_users.sql`: `ALTER TABLE users ADD COLUMN email_verified_at TIMESTAMP
    WITH TIME ZONE;` then the grandfather backfill `UPDATE users SET email_verified_at = created_at WHERE
    email_verified_at IS NULL;` (backfill value = created_at per D-09 discretion; the `WHERE ... IS NULL` guard
    makes it idempotent and a safe no-op on an empty table — VERIFY-05, D-06). Do NOT add a NOT NULL constraint
    (new users register unverified = NULL).
    In `User.kt`, add `@Column(name = "email_verified_at") var emailVerifiedAt: Instant? = null` matching the
    existing nullable-Instant style so ddl-auto validate agrees with V17 (app fails to boot on drift).
  </action>
  <verify>
    <automated>./gradlew compileTestKotlin</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: V16 contains `CREATE TABLE email_verification_tokens` with `token_hash VARCHAR(255) NOT NULL UNIQUE`, nullable `used_at`, and `user_id ... REFERENCES users(id) ON DELETE CASCADE`, plus a unique index on token_hash and a plain index on user_id.
    - Source assertion: V17 adds nullable `email_verified_at TIMESTAMP WITH TIME ZONE` and a backfill `UPDATE users SET email_verified_at = created_at WHERE email_verified_at IS NULL`.
    - Source assertion: `User.kt` has `@Column(name = "email_verified_at") var emailVerifiedAt: Instant? = null`.
    - Parity check: every EmailVerificationToken @Column maps to a matching V16 column; User.emailVerifiedAt maps to the V17 column.
    - `./gradlew compileTestKotlin` succeeds (the schema is exercised end-to-end under Testcontainers by Plan 04/05 integration tests, which boot the Flyway-migrated schema).
  </acceptance_criteria>
  <done>V16 creates email_verification_tokens (unique token_hash), V17 adds the nullable email_verified_at column and grandfathers existing rows, and User.emailVerifiedAt matches V17 exactly (VERIFY-02, VERIFY-05; no schema drift).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB at rest | If the database is dumped, stored values must not be usable as live verification tokens. |
| migration → existing data | The rollout migration must not lock out any pre-existing account. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Verification-token theft via DB leak | high | mitigate | Store only the SHA-256 token_hash; raw token never persisted (Task 1/2) — prohibition authored. |
| T-11-02 | Tampering | Verification-token replay / reuse | high | mitigate | UNIQUE index on token_hash + nullable used_at + atomic markUsed conditional UPDATE (Task 1); single-use race closed (VERIFY-02). |
| T-11-03 | Denial of Service | Grandfather migration locks out legitimate users | high | mitigate | V17 backfills every pre-existing NULL row (WHERE IS NULL) so no current account is gated on rollout (Task 2, VERIFY-05) — prohibition authored. |
| T-11-04 | Denial of Service | Schema drift (entity vs V16/V17) blocks prod boot | high | mitigate | Column parity between entity fields and migrations so ddl-auto validate passes (Task 2). |
| T-11-05 | Tampering | SQL injection on token lookup | medium | mitigate | Spring Data derived findByTokenHash + parameterized @Query markUsed; no string-built SQL (Task 1). |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies this phase — nothing to audit. |
</threat_model>

<verification>
- `./gradlew compileKotlin compileTestKotlin` succeeds.
- Entity @Column names match V16 column names; User.emailVerifiedAt matches V17 (manual/source diff).
- Repository exposes findByTokenHash and an atomic markUsed with a `usedAt IS NULL` guard.
- Migrations apply on Testcontainers boot; deeper persist/lookup/single-use behavior is exercised by the integration tests in Plans 04/05.
</verification>

<success_criteria>
- The email_verification_tokens table + entity + repository exist, store only the SHA-256 hash, and support single indexed findByTokenHash + atomic single-use markUsed (VERIFY-02).
- users gains a nullable email_verified_at column, all pre-existing rows are grandfathered so nobody is locked out (VERIFY-05), and entity/migration schemas stay in sync.
</success_criteria>

<output>
Create `.planning/phases/11-email-verification/11-01-SUMMARY.md` when done.
</output>
