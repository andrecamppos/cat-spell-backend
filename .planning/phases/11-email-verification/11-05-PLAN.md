---
phase: 11-email-verification
plan: 05
type: execute
wave: 4
depends_on: [11-04]
files_modified:
  - src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/auth/AuthIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/auth/RefreshTokenIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/auth/ErrorHandlingIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/common/HealthEndpointIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/discovery/DiscoveryIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/discovery/SwipeMatchIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/discovery/OwnerProfileIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/push/FirebaseHealthIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/push/DeviceTokenIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/profile/CompletenessIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/profile/ProfileIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/profile/PhotoIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/chat/ConversationListIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/chat/ChatIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/match/MatchIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/cat/CatProfileIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/cat/CatPhotoIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/cat/CatCascadeDeleteIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/auth/GrandfatherMigrationTest.kt
autonomous: true
requirements: [VERIFY-03, VERIFY-04, VERIFY-05]
must_haves:
  truths:
    - "BaseIntegrationTest exposes a protected markEmailVerified(email) helper that runs UPDATE users SET email_verified_at = NOW() WHERE email = ? via jdbcTemplate, so tests can promote a freshly-registered (unverified) user past the login hard-gate (VERIFY-03)."
    - "Every existing register-then-authenticate test helper is migrated to the new contract: register now returns 201 with no tokens, so the helper registers, calls markEmailVerified(email), then logs in and reads the access/refresh token(s) from the LOGIN response (VERIFY-03, D-01)."
    - "AuthIntegrationTest asserts the new contracts directly: register returns 201 with a generic body and no tokens; login for an unverified user returns 403 with code EMAIL_NOT_VERIFIED; login after verification returns 200 with tokens (VERIFY-01, VERIFY-03)."
    - "RateLimitIntegrationTest proves POST /api/auth/resend-verification is per-IP rate-limited: from one X-Forwarded-For IP the (capacity+1)th request returns 429, confirming resend-verification is in RateLimitFilter.AUTH_PATHS (VERIFY-04 per-IP)."
    - "GrandfatherMigrationTest proves VERIFY-05: a legacy-style user row with email_verified_at IS NULL, once processed by the V17 backfill statement (UPDATE users SET email_verified_at = created_at WHERE email_verified_at IS NULL), becomes verified (email_verified_at = created_at) and can log in — no pre-existing account is locked out."
    - "The full suite `./gradlew test` is green after migration — no test regresses due to the register/login contract change."
    # Edge (VERIFY-05 / empty) — covered
    - "The V17 backfill statement run against a users table with zero NULL rows affects 0 rows and succeeds (no-op), matching the empty/already-verified case."
    # Edge (VERIFY-05 / adjacency + ordering) — covered
    - "Re-running the backfill statement is idempotent: rows already carrying email_verified_at are untouched (WHERE IS NULL), and the outcome is independent of row order/count."
  prohibitions:
    - "MUST NOT disable or bypass the login verification gate in tests (e.g. a global flag turning the gate off) — tests promote users via markEmailVerified, preserving real gate behavior (VERIFY-03)."
    - "MUST NOT read access/refresh tokens from the register response anymore — register returns no tokens (D-01)."
    - "MUST NOT weaken the grandfather assertion to only checking the column exists — it must prove a NULL-verified user becomes able to log in (VERIFY-05)."
  artifacts:
    - path: "src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt"
      provides: "markEmailVerified(email) shared test helper"
      contains: "markEmailVerified"
    - path: "src/test/kotlin/com/catspell/api/auth/GrandfatherMigrationTest.kt"
      provides: "VERIFY-05 grandfather-backfill proof"
      contains: "GrandfatherMigrationTest"
    - path: "src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt"
      provides: "resend-verification per-IP 429 case"
      contains: "resend-verification"
  key_links:
    - from: "src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt"
      to: "src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql"
      via: "markEmailVerified writes the email_verified_at column added by V17"
      pattern: "email_verified_at"
    - from: "src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt"
      to: "src/main/kotlin/com/catspell/api/common/security/RateLimitFilter.kt"
      via: "asserts resend-verification is in AUTH_PATHS"
      pattern: "resend-verification"
---

<objective>
Migrate the existing integration-test suite to the Phase 11 breaking contract (register returns no
tokens; login hard-gates unverified users — D-01/D-03) and add the two remaining proofs. Introduce a
shared `markEmailVerified(email)` helper, update every register-then-authenticate helper to
register→verify→login, assert the new register/login contracts directly, prove resend-verification is
per-IP rate-limited (VERIFY-04), and prove the grandfather backfill unlocks a legacy NULL-verified
account (VERIFY-05). End state: `./gradlew test` fully green.

Purpose: Keep the whole test suite honest under the new auth contract and close VERIFY-04 (per-IP)
and VERIFY-05.
Output: `markEmailVerified` in BaseIntegrationTest, migrated helpers across ~20 test files, a
resend-verification rate-limit case, and `GrandfatherMigrationTest`.
</objective>

<execution_context>
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/workflows/execute-plan.md
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-email-verification/11-CONTEXT.md
@.planning/phases/11-email-verification/11-PATTERNS.md
@.planning/phases/11-email-verification/11-04-PLAN.md
</context>

<artifacts_this_phase>
Full symbol inventory for Phase 11 (this plan produces the test-migration + remaining-proof subset, marked ★):
- Plans 01-04 produce the production artifacts (entity/repo/migrations, renderer/config, service/gate/exception/whitelist, DTOs/endpoints/register-contract, EmailVerificationIntegrationTest).
- ★ `BaseIntegrationTest.markEmailVerified(email)` test helper
- ★ Migrated register/login helpers across ~20 existing integration tests (register→markEmailVerified→login)
- ★ `RateLimitIntegrationTest` resend-verification per-IP 429 case
- ★ `GrandfatherMigrationTest` (VERIFY-05)
</artifacts_this_phase>

<tasks>

<task type="auto">
  <name>Task 1: Add markEmailVerified helper + migrate auth-package tests to the new contract</name>
  <files>src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt, src/test/kotlin/com/catspell/api/auth/AuthIntegrationTest.kt, src/test/kotlin/com/catspell/api/auth/RefreshTokenIntegrationTest.kt, src/test/kotlin/com/catspell/api/auth/ErrorHandlingIntegrationTest.kt, src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt (self: jdbcTemplate field + cleanDatabase; add helper here)
    - src/test/kotlin/com/catspell/api/auth/AuthIntegrationTest.kt (self: register/login assertions that assume auto-login)
    - src/test/kotlin/com/catspell/api/auth/RefreshTokenIntegrationTest.kt (self: registerAndGetTokens reads tokens from register response, lines 30-39)
    - src/test/kotlin/com/catspell/api/auth/ErrorHandlingIntegrationTest.kt (self: registerUser helper)
    - src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt (self: register→login flow at lines 76, 97)
    - .planning/phases/11-email-verification/11-04-PLAN.md (new register/login/verify contracts)
  </read_first>
  <action>
    In `BaseIntegrationTest.kt` add `protected fun markEmailVerified(email: String) { jdbcTemplate.update("UPDATE
    users SET email_verified_at = NOW() WHERE email = ?", email) }` (jdbcTemplate is already a field). Change its
    visibility only if needed for subclass access.
    Migrate the auth-package helpers to the new contract: `RefreshTokenIntegrationTest.registerAndGetTokens` and
    any register-then-token helper must POST /register (expect 201, ignore the tokenless body), then call
    markEmailVerified(email), then POST /login and read accessToken/refreshToken from the LOGIN response.
    Update `ErrorHandlingIntegrationTest.registerUser` similarly where it relies on post-register auth.
    Update `AuthIntegrationTest` assertions to the new contracts: register → 201 with a generic body and NO
    accessToken/refreshToken; login for an unverified freshly-registered user → 403 with jsonPath code ==
    EMAIL_NOT_VERIFIED; login after markEmailVerified → 200 with tokens. Update `PasswordResetIntegrationTest` so
    its register→login sequences call markEmailVerified before expecting a successful login (its reset behavior
    assertions are otherwise unchanged).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.catspell.api.auth.*"</automated>
  </verify>
  <acceptance_criteria>
    - BaseIntegrationTest has markEmailVerified(email) issuing the email_verified_at UPDATE via jdbcTemplate.
    - AuthIntegrationTest asserts register 201/no-tokens, login-unverified 403 EMAIL_NOT_VERIFIED, login-verified 200/tokens.
    - The auth-package tests (`com.catspell.api.auth.*`) pass green, including EmailVerificationIntegrationTest from Plan 04.
  </acceptance_criteria>
  <done>The shared verify helper exists and all auth-package tests pass under the no-token register + login gate contract (VERIFY-01, VERIFY-03).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate feature-package register helpers to register→verify→login (uniform mechanical edit)</name>
  <files>src/test/kotlin/com/catspell/api/common/HealthEndpointIntegrationTest.kt, src/test/kotlin/com/catspell/api/discovery/DiscoveryIntegrationTest.kt, src/test/kotlin/com/catspell/api/discovery/SwipeMatchIntegrationTest.kt, src/test/kotlin/com/catspell/api/discovery/OwnerProfileIntegrationTest.kt, src/test/kotlin/com/catspell/api/push/FirebaseHealthIntegrationTest.kt, src/test/kotlin/com/catspell/api/push/DeviceTokenIntegrationTest.kt, src/test/kotlin/com/catspell/api/profile/CompletenessIntegrationTest.kt, src/test/kotlin/com/catspell/api/profile/ProfileIntegrationTest.kt, src/test/kotlin/com/catspell/api/profile/PhotoIntegrationTest.kt, src/test/kotlin/com/catspell/api/chat/ConversationListIntegrationTest.kt, src/test/kotlin/com/catspell/api/chat/ChatIntegrationTest.kt, src/test/kotlin/com/catspell/api/match/MatchIntegrationTest.kt, src/test/kotlin/com/catspell/api/cat/CatProfileIntegrationTest.kt, src/test/kotlin/com/catspell/api/cat/CatPhotoIntegrationTest.kt, src/test/kotlin/com/catspell/api/cat/CatCascadeDeleteIntegrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/catspell/api/discovery/DiscoveryIntegrationTest.kt (representative registerAndGetToken helper, lines 30-39 — the exact pattern repeated in all 15 files)
    - src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt (Task 1 — markEmailVerified helper to call)
    - src/test/kotlin/com/catspell/api/auth/RefreshTokenIntegrationTest.kt (Task 1 — reference for reading the token from the login response)
  </read_first>
  <action>
    Apply ONE uniform mechanical transform to each listed file's private `registerAndGetToken(email[, ip])`
    (and equivalents): after the POST /register call (which now returns 201 with no tokens), call
    markEmailVerified(email), then POST /login with the same credentials (preserving any X-Forwarded-For IP
    header the original helper set) and read `accessToken` from the LOGIN response instead of the register
    response. Do not change the helper signatures or the tests' business assertions — only the token-acquisition
    path. This is a repetitive, low-context edit: the same 3-line change per file across all 15 feature tests.
    NOTE (deviation rationale): this task intentionally touches >5 files because a single breaking contract
    change (D-01) invalidated the identical helper duplicated across the suite; the edits are uniform and
    mechanical, so consolidating them here is lower total context than fragmenting into artificial micro-plans.
  </action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>
    - Every listed feature test's register helper acquires its token via login-after-verify, not from the register response.
    - No feature test reads accessToken/refreshToken from a /register response.
    - Full `./gradlew test` is green (no regressions from the contract change).
  </acceptance_criteria>
  <done>All feature-package integration tests pass under the no-token register + login gate contract (VERIFY-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resend-verification per-IP rate-limit case + GrandfatherMigrationTest</name>
  <files>src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt, src/test/kotlin/com/catspell/api/auth/GrandfatherMigrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt (self: postRefresh/postForgotPassword helper + "should rate limit ... endpoint" per-IP pattern; mockMvcWithFilter)
    - src/main/kotlin/com/catspell/api/common/security/RateLimitFilter.kt (Plan 03 — AUTH_PATHS now includes resend-verification)
    - src/test/kotlin/com/catspell/api/BaseIntegrationTest.kt (jdbcTemplate + markEmailVerified for the grandfather test)
    - src/main/resources/db/migration/V17__add_email_verified_at_to_users.sql (Plan 01 — the backfill statement to exercise)
  </read_first>
  <behavior>
    - VERIFY-04 (per-IP boundary): from one X-Forwarded-For IP, requests up to capacity succeed and the (capacity+1)th POST /api/auth/resend-verification returns 429 — proving resend-verification is in AUTH_PATHS.
    - VERIFY-05 (grandfather): insert (via jdbcTemplate) a users row simulating a pre-migration account with email_verified_at IS NULL and a known created_at; run the exact V17 backfill statement (UPDATE users SET email_verified_at = created_at WHERE email_verified_at IS NULL); assert the row's email_verified_at is now = created_at (grandfathered) and that logging in as that user succeeds (403 gate no longer triggers).
    - VERIFY-05 (idempotent/empty edge): running the backfill again affects 0 additional rows and does not overwrite an already-set email_verified_at.
  </behavior>
  <action>
    In `RateLimitIntegrationTest` add a `postResendVerification(ip)` helper mirroring the existing per-IP helpers
    (POST /api/auth/resend-verification with a JSON { email } body + X-Forwarded-For header) and a `should rate
    limit resend-verification endpoint` test firing capacity+1 requests from one IP against mockMvcWithFilter,
    expecting the final one to be status().isTooManyRequests.
    Create `GrandfatherMigrationTest : BaseIntegrationTest()` (@SpringBootTest @AutoConfigureMockMvc) proving the
    <behavior> grandfather cases: use jdbcTemplate to INSERT a legacy user (with a BCrypt-hashed password, a
    fixed created_at, email_verified_at NULL), execute the V17 backfill UPDATE, assert email_verified_at = created_at,
    then POST /login for that user and assert 200 with tokens (the login gate passes). Add an idempotency assertion
    (second run affects 0 rows / leaves the value unchanged). Stub EmailSender with a MockK @Primary bean if the
    context requires it (no real sends, EMAIL-02).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.catspell.api.common.RateLimitIntegrationTest" --tests "com.catspell.api.auth.GrandfatherMigrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - The resend-verification per-IP case passes: (capacity+1)th request from one IP → 429.
    - GrandfatherMigrationTest proves a NULL-verified legacy user is backfilled to created_at and can then log in (VERIFY-05).
    - The backfill idempotency assertion passes (re-run affects 0 rows / no overwrite).
    - Existing RateLimitIntegrationTest cases remain green.
  </acceptance_criteria>
  <done>resend-verification is proven per-IP rate-limited (VERIFY-04) and the grandfather backfill is proven to unlock legacy accounts idempotently (VERIFY-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise the real login gate and real migrations; must not bypass security to pass. |
| migration → legacy data | The rollout backfill must unlock every pre-existing account without overwriting set values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-21 | Denial of Service | Grandfather migration locks out legitimate users | high | mitigate | GrandfatherMigrationTest proves a NULL-verified legacy user is backfilled and can log in (Task 3, VERIFY-05). |
| T-11-22 | Denial of Service | resend-verification email-bombing (missed AUTH_PATHS) | high | mitigate | Per-IP 429 boundary test proves AUTH_PATHS membership (Task 3, VERIFY-04). |
| T-11-23 | Elevation of Privilege | Tests bypass the login gate, hiding a real auth regression | high | mitigate | Tests promote users via markEmailVerified (real column write), never a gate-off flag (Task 1/2) — prohibition authored. |
| T-11-24 | Spoofing | Real network email during CI | medium | mitigate | EmailSender stubbed via MockK @Primary where needed; no network (Task 3). |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies this phase — nothing to audit. |
</threat_model>

<verification>
- `./gradlew test` is fully green (all packages, including migrated feature tests + the two new tests).
- Source review: no test reads tokens from a /register response; the login gate is exercised (not bypassed); grandfather test proves a login-unlock, not just column existence.
</verification>

<success_criteria>
- The entire integration suite passes under the no-token register + login hard-gate contract; resend-verification is proven per-IP rate-limited (VERIFY-04); and the grandfather backfill is proven to unlock legacy NULL-verified accounts idempotently so no current user is locked out (VERIFY-03, VERIFY-04, VERIFY-05).
</success_criteria>

<output>
Create `.planning/phases/11-email-verification/11-05-SUMMARY.md` when done.
</output>
