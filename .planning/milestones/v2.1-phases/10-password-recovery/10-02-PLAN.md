---
phase: 10-password-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt
  - src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt
  - src/main/resources/db/migration/V15__create_password_reset_tokens_table.sql
autonomous: true
requirements: [RECOV-05]
must_haves:
  truths:
    - "A password_reset_tokens table exists (via Flyway V15) with a UUID PK, a user_id FK to users(id) ON DELETE CASCADE, a unique token_hash column, an expires_at timestamp, a nullable used_at timestamp, and a created_at timestamp."
    - "The PasswordResetToken JPA entity column names/types exactly match the V15 migration so Hibernate ddl-auto validate (prod) and create-drop (tests) both agree on the schema."
    - "PasswordResetTokenRepository exposes findByTokenHash(tokenHash) for direct indexed redemption lookup (no scan-and-match)."
    # Edge (RECOV-05 / precision) — covered: deterministic hash enables exact lookup
    - "token_hash stores a deterministic SHA-256 digest (fixed-width hex), so an identical raw token always maps to the same stored value and is found by a single unique-index lookup (no precision/collision ambiguity)."
  prohibitions:
    - "Never store the raw reset token in the database — only the SHA-256 hash is persisted in token_hash."
    - "Never make token_hash nullable or non-unique — single-use redemption depends on a unique indexed lookup."
  artifacts:
    - path: "src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt"
      provides: "JPA entity for the hashed single-use reset token"
      contains: "password_reset_tokens"
    - path: "src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt"
      provides: "Spring Data repository with findByTokenHash"
      contains: "findByTokenHash"
    - path: "src/main/resources/db/migration/V15__create_password_reset_tokens_table.sql"
      provides: "Flyway migration creating password_reset_tokens + indexes"
      contains: "CREATE TABLE password_reset_tokens"
  key_links:
    - from: "src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt"
      to: "src/main/resources/db/migration/V15__create_password_reset_tokens_table.sql"
      via: "entity @Column names/types must match migration DDL (schema parity, Pitfall #4)"
      pattern: "token_hash"
    - from: "src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt"
      to: "src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt"
      via: "JpaRepository<PasswordResetToken, UUID>"
      pattern: "PasswordResetToken"
---

<objective>
Create the durable, hashed, single-use reset-token store (RECOV-05 storage half): a
`PasswordResetToken` JPA entity + repository + Flyway `V15` migration, mirroring the proven
`RefreshToken` + `V2` + `RefreshTokenRepository` pattern. Tokens are stored ONLY as a SHA-256
hash (RECOV-05, D-07 discretion) — the raw token lives only in the email (Plan 01).

Purpose: Give Plan 03's service a place to persist an issued token's hash and to look it up
atomically by hash on redemption.
Output: `auth/model/PasswordResetToken.kt`, `auth/model/PasswordResetTokenRepository.kt`,
`db/migration/V15__create_password_reset_tokens_table.sql`.
</objective>

<execution_context>
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/workflows/execute-plan.md
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-password-recovery/10-CONTEXT.md
@.planning/phases/10-password-recovery/10-RESEARCH.md
@.planning/phases/10-password-recovery/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the PasswordResetToken entity + repository (mirror RefreshToken)</name>
  <files>src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt, src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt</files>
  <read_first>
    - src/main/kotlin/com/catspell/api/auth/model/RefreshToken.kt (exact template: UUID PK, @ManyToOne user, timestamps, equals/hashCode by id)
    - src/main/kotlin/com/catspell/api/auth/model/RefreshTokenRepository.kt (exact template: derived-query naming)
    - src/main/kotlin/com/catspell/api/auth/model/User.kt (the FK target)
    - .planning/phases/10-password-recovery/10-PATTERNS.md (PasswordResetToken.kt / repository copy notes)
  </read_first>
  <action>
    Create `@Entity @Table(name = "password_reset_tokens") class PasswordResetToken` in package
    `com.catspell.api.auth.model`, copying `RefreshToken.kt` structure: `@Id @GeneratedValue(strategy =
    GenerationType.UUID) var id: UUID? = null`; `@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name =
    "user_id", nullable = false) var user: User`; `@Column(name = "token_hash", nullable = false, unique =
    true) var tokenHash: String`; `@Column(name = "expires_at", nullable = false) var expiresAt: Instant`;
    `@Column(name = "used_at") var usedAt: Instant? = null` (nullable single-use marker — replaces
    RefreshToken's revoked/replacedBy); `@Column(name = "created_at", nullable = false, updatable = false)
    var createdAt: Instant = Instant.now()`. Keep the `equals`/`hashCode` by-id override verbatim from
    RefreshToken.kt. Store ONLY the hash — there is no raw-token column.
    Create `interface PasswordResetTokenRepository : JpaRepository<PasswordResetToken, UUID>` with
    `fun findByTokenHash(tokenHash: String): PasswordResetToken?` (indexed redemption lookup) and
    `fun findAllByUserAndUsedAtIsNull(user: User): List<PasswordResetToken>` (lets Plan 03 optionally
    invalidate a user's prior unused tokens on re-request).
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: entity is annotated `@Table(name = "password_reset_tokens")` with columns `token_hash` (unique, non-null), `expires_at` (non-null), `used_at` (nullable), `user_id` FK, and `created_at`.
    - Source assertion: no raw-token column exists (only `tokenHash`).
    - Source assertion: repository exposes `findByTokenHash` returning a nullable entity.
    - `./gradlew compileKotlin` succeeds.
  </acceptance_criteria>
  <done>The hashed single-use reset-token entity + repository compile and expose an indexed `findByTokenHash` lookup (RECOV-05).</done>
</task>

<task type="auto">
  <name>Task 2: Create the V15 Flyway migration (schema parity with the entity)</name>
  <files>src/main/resources/db/migration/V15__create_password_reset_tokens_table.sql</files>
  <read_first>
    - src/main/resources/db/migration/V2__create_refresh_tokens_table.sql (exact DDL template)
    - src/main/kotlin/com/catspell/api/auth/model/PasswordResetToken.kt (Task 1 — columns MUST match this)
    - .planning/phases/10-password-recovery/10-RESEARCH.md (Pitfall #4 — entity/migration drift; tests use ddl-auto create-drop with Flyway disabled while prod uses ddl-auto validate)
  </read_first>
  <action>
    Create `V15__create_password_reset_tokens_table.sql` (next number after the existing V14) mirroring
    `V2__create_refresh_tokens_table.sql`: `CREATE TABLE password_reset_tokens (id UUID PRIMARY KEY DEFAULT
    gen_random_uuid(), user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, token_hash VARCHAR(255)
    NOT NULL UNIQUE, expires_at TIMESTAMP WITH TIME ZONE NOT NULL, used_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW());`. Add `CREATE INDEX idx_password_reset_tokens_user_id
    ON password_reset_tokens(user_id);` and `CREATE UNIQUE INDEX idx_password_reset_tokens_token_hash ON
    password_reset_tokens(token_hash);`. Column names/types MUST exactly match the entity `@Column` mappings
    from Task 1 so Hibernate `validate` (prod, application.yml:9) and `create-drop` (tests) agree
    (RESEARCH Pitfall #4). This is the [BLOCKING] schema artifact — Flyway auto-runs on app boot; there is no
    separate push command (JPA/Flyway stack).
  </action>
  <verify>
    <automated>./gradlew compileTestKotlin</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `V15__...sql` contains `CREATE TABLE password_reset_tokens` with `token_hash VARCHAR(255) NOT NULL UNIQUE`, `used_at TIMESTAMP WITH TIME ZONE` (nullable), and `user_id ... REFERENCES users(id) ON DELETE CASCADE`.
    - Source assertion: a unique index on `token_hash` and a plain index on `user_id` are created.
    - Parity check: every entity `@Column(name = ...)` from Task 1 maps to a column of matching type in V15 (token_hash, expires_at, used_at, user_id, created_at, id).
    - `./gradlew compileTestKotlin` succeeds (schema is exercised end-to-end by the integration test in Plan 04, which boots the entity-derived schema under Testcontainers).
  </acceptance_criteria>
  <done>V15 creates `password_reset_tokens` with a unique `token_hash` index, matching the entity exactly (RECOV-05; Pitfall #4 avoided).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB at rest | If the database is dumped, stored values must not be usable as live reset tokens. |
| app → DB | Token lookups cross into the persistence tier; must be parameterized and by hash only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Information Disclosure | Token theft via DB leak | high | mitigate | Store only the SHA-256 `token_hash`; raw token never persisted (Task 1/2) — prohibition authored. |
| T-10-05 | Tampering | Token replay / collision | high | mitigate | `UNIQUE` index on `token_hash` (Task 2) + nullable `used_at` single-use marker; atomic consume lives in Plan 03. |
| T-10-06 | Tampering | SQL injection on token lookup | medium | mitigate | Spring Data derived `findByTokenHash` is parameterized; no raw SQL (Task 1). |
| T-10-07 | Denial of Service | Schema drift (entity vs V15) blocks prod boot | high | mitigate | Column parity between entity and V15 so `ddl-auto: validate` passes in prod (Task 2, Pitfall #4). |
| T-10-SC | Tampering | package installs | high | accept | No new dependencies this phase — nothing to audit. |
</threat_model>

<verification>
- `./gradlew compileKotlin compileTestKotlin` succeeds.
- Entity `@Column` names and V15 column names are identical (manual/source diff).
- Repository exposes `findByTokenHash`.
- Deeper behavior (persist a hashed row, look it up by hash) is exercised by `PasswordResetIntegrationTest` in Plan 04 under Testcontainers.
</verification>

<success_criteria>
- The `password_reset_tokens` table + entity + repository exist, store only the SHA-256 hash, and support single indexed `findByTokenHash` redemption lookup (RECOV-05).
- Entity and V15 stay in sync so both test (create-drop) and prod (validate) schemas agree.
</success_criteria>

<output>
Create `.planning/phases/10-password-recovery/10-02-SUMMARY.md` when done.
</output>
