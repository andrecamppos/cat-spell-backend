---
phase: 10-password-recovery
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - src/main/kotlin/com/catspell/api/auth/model/AuthDtos.kt
  - src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt
  - src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt
  - src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt
autonomous: true
requirements: [RECOV-01, RECOV-03, RECOV-04, RECOV-05, RECOV-06, RECOV-07]
must_haves:
  truths:
    - "POST /api/auth/forgot-password accepts { email }, is reachable without a JWT, and returns 202 Accepted with a fixed generic body (D-05, RECOV-01)."
    - "POST /api/auth/forgot-password returns a byte-identical response (status + body) whether the email is registered or not (RECOV-04, D-05)."
    - "POST /api/auth/reset-password accepts { token, newPassword }, and on a valid token returns 200 with NO tokens in the body; the user can then log in with the new password (RECOV-03, D-07)."
    - "A reused (already-used) reset token and an expired reset token are both rejected with 401 (RECOV-05)."
    - "The stored token row holds a hash, not the raw token that appeared in the email (RECOV-05)."
    - "After a successful reset, a refresh token issued before the reset is rejected (RECOV-06)."
    - "Firing more than the per-IP capacity of forgot-password requests from one IP yields 429 (RECOV-07 per-IP), proving the endpoint is in AUTH_PATHS."
    # Edge (RECOV-01 / concurrency) — covered
    - "Concurrent forgot-password requests each independently receive 202 (no shared-state error)."
    # Edge (RECOV-03 / empty) — covered
    - "reset-password with an empty/blank newPassword is rejected by validation (400 @Size(min=8)); with an empty/unknown token it is rejected as invalid (401)."
    # Edge (RECOV-07 / boundary) — covered
    - "The Nth per-IP forgot-password request within the window succeeds and the (N+1)th returns 429 (boundary)."
    # Edge (RECOV-03 / adjacency) — backstop
    - statement: "Two reset-password submissions with the same valid token — the first succeeds, the second (now touching an already-used token) is rejected 401 (single-use holds at the exact adjacency)."
      verification: backstop
  prohibitions:
    - "Never include access or refresh tokens (or any session) in the reset-password 200 response body (D-07, no auto-login)."
    - "Never return a status or body from forgot-password that differs by whether the email is registered (RECOV-04)."
    - "Never perform a real network email send in the integration tests — stub EmailSender with a MockK bean."
    # Edge (RECOV-03 / ordering) — flagged assumption (see <assumptions>)
    # Edge (RECOV-02 / unclassified) — flagged assumption (see <assumptions>)
  artifacts:
    - path: "src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt"
      provides: "forgotPassword (202) + resetPassword (200) endpoints"
      contains: "forgot-password"
    - path: "src/main/kotlin/com/catspell/api/auth/model/AuthDtos.kt"
      provides: "ForgotPasswordRequest, ResetPasswordRequest, GenericMessageResponse DTOs"
      contains: "ForgotPasswordRequest"
    - path: "src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt"
      provides: "End-to-end proof of RECOV-01..06"
      contains: "PasswordResetIntegrationTest"
  key_links:
    - from: "src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt"
      to: "src/main/kotlin/com/catspell/api/auth/service/PasswordResetService.kt"
      via: "forgotPassword calls passwordResetService.requestReset"
      pattern: "requestReset"
    - from: "src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt"
      to: "src/main/kotlin/com/catspell/api/auth/service/AuthService.kt"
      via: "resetPassword calls authService.resetPassword"
      pattern: "resetPassword"
---

<objective>
Expose the HTTP surface (RECOV-01, RECOV-03) — `POST /api/auth/forgot-password` (202 generic,
D-05) and `POST /api/auth/reset-password` (200, no tokens, D-07) on the existing `AuthController` —
with `@Valid` DTOs, and prove the whole flow end-to-end with integration tests covering RECOV-01..07.

Purpose: Turn the Plan 03 service logic into the public, validated, tested recovery API.
Output: DTO additions in `AuthDtos.kt`, two endpoints in `AuthController.kt`,
`PasswordResetIntegrationTest.kt`, and an extended `RateLimitIntegrationTest.kt`.
</objective>

<execution_context>
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/workflows/execute-plan.md
@/Users/andre.campos/ideas/cat-spell-backend/.windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-password-recovery/10-CONTEXT.md
@.planning/phases/10-password-recovery/10-RESEARCH.md
@.planning/phases/10-password-recovery/10-PATTERNS.md
@.planning/phases/10-password-recovery/10-03-PLAN.md
</context>

<assumptions>
Surfaced from the spec-less edge probe (not silently dropped):
- **RECOV-03 (ordering)** — "When elements compare equal, is output order specified and stable?" is
  **not applicable**: a single reset produces no ordered collection output. Recorded as a flagged
  assumption; no acceptance criterion authored.
- **RECOV-02 (unclassified)** and **EMAIL-01 (unclassified, surfaced in Plan 01)** — the probe left
  these unclassified; treated as flagged assumptions rather than fabricated criteria. RECOV-02's
  substantive behavior (single-use, time-limited link issued + emailed) IS covered by Plan 03 Task 1
  and this plan's integration test; the unclassified edge itself carries no additional predicate.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Add DTOs + forgot/reset endpoints on AuthController</name>
  <files>src/main/kotlin/com/catspell/api/auth/model/AuthDtos.kt, src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt</files>
  <read_first>
    - src/main/kotlin/com/catspell/api/auth/model/AuthDtos.kt (RegisterRequest @Email / @Size validation pattern)
    - src/main/kotlin/com/catspell/api/auth/controller/AuthController.kt (self: @SecurityRequirements + @Valid + ResponseEntity conventions, lines 21-40)
    - src/main/kotlin/com/catspell/api/auth/service/PasswordResetService.kt (Plan 03 — requestReset)
    - src/main/kotlin/com/catspell/api/auth/service/AuthService.kt (Plan 03 — resetPassword)
    - .planning/phases/10-password-recovery/10-RESEARCH.md (Code Examples → DTOs + Controller)
  </read_first>
  <action>
    In `AuthDtos.kt` add: `data class ForgotPasswordRequest(@field:Email(message = "must be a valid email
    address") val email: String)`; `data class ResetPasswordRequest(val token: String, @field:Size(min = 8,
    message = "must be at least 8 characters") val newPassword: String)`; `data class GenericMessageResponse(val
    message: String)`. In `AuthController` inject `PasswordResetService` (add to the constructor alongside the
    existing `AuthService`). Add `@SecurityRequirements @PostMapping("/forgot-password") fun
    forgotPassword(@Valid @RequestBody request: ForgotPasswordRequest): ResponseEntity<GenericMessageResponse>`
    that calls `passwordResetService.requestReset(request.email)` and ALWAYS returns
    `ResponseEntity.accepted().body(GenericMessageResponse(<generic message>))` (202, D-05 — identical
    regardless of existence). Add `@SecurityRequirements @PostMapping("/reset-password") fun
    resetPassword(@Valid @RequestBody request: ResetPasswordRequest): ResponseEntity<Void>` that calls
    `authService.resetPassword(request.token, request.newPassword)` and returns `ResponseEntity.ok().build()`
    (200, NO body/tokens, D-07). Rely on the existing `GlobalExceptionHandler` for 400 (validation) and 401
    (InvalidTokenException) — add no new handler.
  </action>
  <verify>
    <automated>./gradlew compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `forgotPassword` is annotated `@SecurityRequirements` + `@PostMapping("/forgot-password")`, returns `ResponseEntity.accepted()` (202) unconditionally with a `GenericMessageResponse`.
    - Source assertion: `resetPassword` returns `ResponseEntity.ok().build()` (200) with `Void` body (no `AuthResponse`).
    - Source assertion: `ResetPasswordRequest.newPassword` carries `@Size(min = 8)`; `ForgotPasswordRequest.email` carries `@Email`.
    - `./gradlew compileKotlin` succeeds.
  </acceptance_criteria>
  <done>Both endpoints exist with validated DTOs, returning 202-generic (D-05) and 200-no-tokens (D-07) (RECOV-01, RECOV-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PasswordResetIntegrationTest — RECOV-01..06 end to end</name>
  <files>src/test/kotlin/com/catspell/api/auth/PasswordResetIntegrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/catspell/api/auth/RefreshTokenIntegrationTest.kt (exact harness: @SpringBootTest @AutoConfigureMockMvc, registerAndGetTokens helper, force-expiry-via-repo lines 113-127)
    - src/test/kotlin/com/catspell/api/push/TokenPruningTest.kt (MockK @TestConfiguration @Bean @Primary pattern for stubbing a provider)
    - src/main/kotlin/com/catspell/api/email/service/EmailSender.kt (Plan 01 — the bean to stub/capture)
    - src/main/kotlin/com/catspell/api/auth/model/PasswordResetTokenRepository.kt (Plan 02)
    - .planning/phases/10-password-recovery/10-PATTERNS.md (PasswordResetIntegrationTest copy notes)
  </read_first>
  <behavior>
    - RECOV-01: POST /forgot-password with { email } for a registered user (no JWT) → 202 with the generic body.
    - RECOV-04: POST /forgot-password for a registered vs an unregistered email → identical status (202) and identical body bytes.
    - RECOV-02/05: after a registered forgot request, a PasswordResetToken row exists whose stored value != the raw token captured from the stubbed EmailSender (stored hashed).
    - RECOV-03: POST /reset-password with the captured raw token + new password → 200 no body; subsequent POST /login with the new password → success; login with the old password → 401.
    - RECOV-05: a second /reset-password with the same (now-used) token → 401; a token force-expired via the repo → 401.
    - RECOV-06: a refresh token issued before the reset → rejected (401) on /refresh after the reset.
    - Concurrency edge (RECOV-01): parallel forgot requests each return 202.
    - Empty edge (RECOV-03): /reset-password with blank newPassword → 400; with an unknown/blank token → 401.
  </behavior>
  <action>
    Create `PasswordResetIntegrationTest : BaseIntegrationTest()` (`@SpringBootTest @AutoConfigureMockMvc`),
    mirroring `RefreshTokenIntegrationTest`. Add a `@TestConfiguration` exposing `@Bean @Primary fun
    emailSender(): EmailSender = mockk(relaxed = true)` and capture the `EmailMessage` (or the raw token) via
    MockK `slot`/`every` so the test can read the emailed raw token WITHOUT a real send (EMAIL-02 discipline).
    Autowire `mockMvc`, `objectMapper`, `passwordResetTokenRepository`, `userRepository`,
    `refreshTokenRepository`. Implement the cases in <behavior>. Reuse the force-expiry-via-repo idiom
    (RefreshTokenIntegrationTest.kt:113-127) to make used/expired token cases. Assert the reset 200 response
    body carries no `accessToken`/`refreshToken`.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.catspell.api.auth.PasswordResetIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - All <behavior> cases pass green.
    - The stored token value differs from the raw token captured from the stubbed sender (RECOV-05).
    - The reset 200 body contains no session tokens (D-07).
    - No real network email send occurs (EmailSender is a MockK bean).
  </acceptance_criteria>
  <done>RECOV-01, RECOV-02, RECOV-03, RECOV-04, RECOV-05, RECOV-06 are proven end to end with a stubbed email sender.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend RateLimitIntegrationTest — forgot-password per-IP 429 (RECOV-07)</name>
  <files>src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt</files>
  <read_first>
    - src/test/kotlin/com/catspell/api/common/RateLimitIntegrationTest.kt (self: postRefresh helper lines 44-47 + "should rate limit refresh endpoint" lines 101-107)
    - src/main/kotlin/com/catspell/api/common/security/RateLimitFilter.kt (Plan 03 — AUTH_PATHS now includes forgot-password)
    - .planning/phases/10-password-recovery/10-PATTERNS.md (RateLimitIntegrationTest extend notes)
  </read_first>
  <behavior>
    - RECOV-07 (per-IP boundary): from one X-Forwarded-For IP, the requests up to capacity succeed and the (capacity+1)th POST /api/auth/forgot-password returns 429 — proving /forgot-password is in AUTH_PATHS.
  </behavior>
  <action>
    Add a `postForgotPassword(ip)` helper mirroring `postRefresh` (RateLimitIntegrationTest.kt:44-47) that POSTs
    `/api/auth/forgot-password` with a JSON `{ "email": "..." }` body and an `X-Forwarded-For` header. Add a
    `should rate limit forgot-password endpoint` test that fires capacity+1 requests from one IP against
    `mockMvcWithFilter` and expects the final one to be `status().isTooManyRequests` (mirror the refresh test at
    lines 101-107). The per-email guard's "still 202, never 429" behavior is asserted in
    PasswordResetIntegrationTest (Task 2), not here (that guard is email-keyed, not IP-keyed).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.catspell.api.common.RateLimitIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - The new forgot-password rate-limit case passes: (capacity+1)th request from one IP → 429.
    - Existing RateLimitIntegrationTest cases remain green.
  </acceptance_criteria>
  <done>forgot-password is proven per-IP rate-limited (RECOV-07 per-IP), confirming the AUTH_PATHS whitelist edit from Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → HTTP API | Untrusted request bodies validated by Jakarta Bean Validation before service logic runs. |
| API → client (response) | Response must not carry session tokens (reset) or existence signals (forgot). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16 | Information Disclosure | Enumeration via distinct forgot response | high | mitigate | Identical 202 body asserted for registered vs unregistered (Task 2, RECOV-04) — prohibition authored. |
| T-10-17 | Elevation of Privilege | Auto-login / token leak on reset response | high | mitigate | reset returns 200 Void; test asserts no tokens in body (Task 1/2, D-07) — prohibition authored. |
| T-10-18 | Denial of Service | Unlimited forgot-password (missed AUTH_PATHS) | high | mitigate | Per-IP 429 boundary test (Task 3, RECOV-07). |
| T-10-19 | Spoofing | Real network email during CI reveals infra / flakes | medium | mitigate | EmailSender stubbed via MockK @Primary bean; no network (Task 2) — prohibition authored. |
| T-10-20 | Input Validation | Malformed email / short password | medium | mitigate | `@Email` + `@Size(min=8)` DTO validation → 400 via GlobalExceptionHandler (Task 1/2). |
| T-10-SC | Tampering | package installs | high | accept | No new dependencies this phase. |
</threat_model>

<verification>
- `./gradlew test --tests "com.catspell.api.auth.PasswordResetIntegrationTest" --tests "com.catspell.api.common.RateLimitIntegrationTest"` is green.
- Full-suite gate before verify: `./gradlew test` green.
- Manual/source review: reset-password returns no tokens; forgot-password body is fixed/generic.
</verification>

<success_criteria>
- The forgot/reset endpoints are public, validated, enumeration-safe (202 generic), and reset returns 200 with no tokens (RECOV-01, RECOV-03, RECOV-04; D-05, D-07).
- Reused/expired tokens are rejected 401, tokens are stored hashed, all sessions revoked on reset, and forgot-password is per-IP rate-limited (RECOV-05, RECOV-06, RECOV-07).
</success_criteria>

<output>
Create `.planning/phases/10-password-recovery/10-04-SUMMARY.md` when done.
</output>
